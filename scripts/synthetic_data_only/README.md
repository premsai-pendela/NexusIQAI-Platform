These scripts generate the demo dataset used in NexusIQ's portfolio deployment.
They are NOT needed for connecting NexusIQ to real company data.
For real deployments, use database/ingestion_pipeline.py to ingest your own PDFs.
